# API URL (backend endpoint)
# For local dev with backend on localhost:8000
VITE_API_URL=http://localhost:8000/api

# For production on Render, this will be set by the render.yaml referencing the backend service
# Example: https://ifugao-bmi-backend.onrender.com/api
