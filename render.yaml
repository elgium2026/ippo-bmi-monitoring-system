services:
  - type: web
    name: Ifugao BMI Backend
    dockerfilePath: backend/Dockerfile
    branch: main
    plan: free
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: bmi_monitor.settings
